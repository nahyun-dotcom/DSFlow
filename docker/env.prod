# 운영환경 설정 (docker-compose.prod.yml 용)

# PostgreSQL 외부 서버 설정
DB_HOST=your-postgres-server.com
DB_PORT=5432
DB_NAME=dsflow
DB_USERNAME=dsflow
DB_PASSWORD=your-secure-password

# Slack 알림 설정 (선택사항)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# 기타 운영 설정
SPRING_PROFILES_ACTIVE=prod 